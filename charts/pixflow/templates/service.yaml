apiVersion: v1
kind: Service
metadata:
  name: {{ include "pixflow.fullname" . }}
  labels:
    {{- include "pixflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pixflow.selectorLabels" . | nindent 4 }}
